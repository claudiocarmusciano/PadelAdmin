# Solo para desarrollo local (npm run dev). El build de producción NO usa este archivo
# y cae al default "/api" (mismo origen, frontend servido por el backend).
VITE_API_BASE_URL=http://localhost:8080/api
